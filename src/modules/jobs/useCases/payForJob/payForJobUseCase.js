const {getUnpaidJobs} = require("../../jobRepository");

const payForJobUseCase = async(Job, Contract, job_id, profile_id) => {
    //TODO Implement this function
    return null;
}

module.exports = payForJobUseCase